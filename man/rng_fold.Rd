% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rngat.R
\name{rng_fold}
\alias{rng_fold}
\title{Fold data into a key}
\usage{
rng_fold(key, data)
}
\arguments{
\item{key}{An \code{rng_key} vector.}

\item{data}{A character, integer, double, logical or raw vector. Numeric
values must be whole numbers; integer and double vectors holding the
same values (\code{1L} and \code{1}) derive the same key. All other type
distinctions matter: \code{rng_fold(key, "1")} and \code{rng_fold(key, 1)} differ.}
}
\value{
An \code{rng_key} vector with the same length as \code{key}.
}
\description{
\code{rng_fold()} deterministically derives a key from \code{key} and simple data.
This is useful for structured reproducibility, for example deriving a key
from names such as \code{"chain 3"} or counters such as \code{10L}.
}
\examples{
key <- rng_key(42L)
layer_key <- rng_fold(key, "layer1")
}
