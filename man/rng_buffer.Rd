% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zurand.R
\name{rng_buffer}
\alias{rng_buffer}
\title{Allocate a buffer for the in-place fills}
\usage{
rng_buffer(n)
}
\arguments{
\item{n}{A single non-negative whole number: the buffer length.}
}
\value{
A numeric vector of length \code{n}.
}
\description{
\code{rng_buffer()} creates a numeric vector to be filled repeatedly by
\code{\link[=rng_fill_uniform]{rng_fill_uniform()}} and \code{\link[=rng_fill_normal]{rng_fill_normal()}}. It is an ordinary numeric
vector carrying one marker attribute, which \code{\link[=rng_fill_uniform]{rng_fill_uniform()}} requires:
a vector that was not made for overwriting cannot be passed to them by
accident. Unlike \code{numeric(n)} it is not zero-filled, since the fill
overwrites it immediately.
}
\examples{
buf <- rng_buffer(1000)
rng_fill_uniform(buf, rng_key(42L))
mean(buf)
}
\seealso{
\code{\link[=rng_fill_uniform]{rng_fill_uniform()}} for the hazard these buffers carry.
}
