% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zurand.R
\name{rng_threads}
\alias{rng_threads}
\title{Control zurand's thread usage}
\usage{
rng_threads(threads = NULL)
}
\arguments{
\item{threads}{\code{NULL} to query the effective maximum, a single whole
number of at least 1 to cap zurand's thread use, or \code{0} to remove the
cap. Values above the machine's thread count are allowed and
equivalent to no cap.}
}
\value{
Querying returns the effective maximum thread count as an
integer. Setting invisibly returns the previous \emph{configured} cap,
with \code{0L} meaning "no cap was set", so \code{old <- rng_threads(1L)}
followed by \code{rng_threads(old)} restores the exact prior
configuration -- including the uncapped state, which keeps tracking
later changes to the process-wide OpenMP maximum.
}
\description{
\code{rng_threads()} reports the maximum number of OpenMP threads the samplers
may use; \code{rng_threads(threads)} caps it for the rest of the session. The
cap applies only to zurand's own parallel fills, not to other OpenMP code
in the process. Results never depend on the thread count, so this is a
performance control, not a reproducibility one. In builds without OpenMP
the value is always 1 and setting a cap has no effect.
}
\details{
Draws below an internal size threshold always run single-threaded.
}
\examples{
rng_threads()
old <- rng_threads(1L)
rng_threads(old)
}
