% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rngat.R
\name{rng_threads}
\alias{rng_threads}
\title{Control rngat's thread usage}
\usage{
rng_threads(threads = NULL)
}
\arguments{
\item{threads}{\code{NULL} to query, or a single whole number of at least 1.
Values above the machine's thread count are allowed and equivalent to
no cap.}
}
\value{
The previous maximum as an integer: visibly when querying,
invisibly when setting, so \code{old <- rng_threads(1L)} supports restoring
with \code{rng_threads(old)}.
}
\description{
\code{rng_threads()} reports the maximum number of OpenMP threads the samplers
may use; \code{rng_threads(threads)} caps it for the rest of the session. The
cap applies only to rngat's own parallel fills, not to other OpenMP code
in the process. Results never depend on the thread count, so this is a
performance control, not a reproducibility one. In builds without OpenMP
the value is always 1 and setting a cap has no effect.
}
\details{
Draws below an internal size threshold always run single-threaded.
}
\examples{
rng_threads()
old <- rng_threads(1L)
rng_threads(old)
}
