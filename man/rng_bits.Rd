% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rngat.R
\name{rng_bits}
\alias{rng_bits}
\title{Draw low-level random bits}
\usage{
rng_bits(key, n = 1L, bits = 32L)
}
\arguments{
\item{key}{An \code{rng_key} vector.}

\item{n}{A single non-negative whole number. Defaults to one draw per key.}

\item{bits}{Either \code{32L} or \code{64L}.}
}
\value{
A numeric vector or matrix for 32-bit words, or a character vector or matrix
for 64-bit words.
}
\description{
\code{rng_bits()} exposes the raw generator stream. With \code{bits = 32}, it returns
unsigned 32-bit words stored exactly in a numeric vector. With \code{bits = 64},
it returns fixed-width hexadecimal strings so no bits are lost to R's
numeric representation.
}
\examples{
key <- rng_key(42L)
rng_bits(key)
rng_bits(key, 5L)
}
