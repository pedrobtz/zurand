% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rngat.R
\name{bits_seq}
\alias{bits_seq}
\alias{runif_seq}
\alias{rnorm_seq}
\title{Draw a contiguous run of random values}
\usage{
bits_seq(key, start, len, domain = NULL)

runif_seq(key, start, len, domain = NULL)

rnorm_seq(key, start, len, domain = NULL)
}
\arguments{
\item{key}{An \code{rngat_key}, from \code{\link[=rng_key]{rng_key()}} or \code{\link[=fold_in]{fold_in()}}.}

\item{start}{A single whole number: the first index of the run.}

\item{len}{A single non-negative whole number: how many values.}

\item{domain}{Optional single whole number selecting an independent
stream (default \code{NULL} is domain 0).}
}
\value{
A double vector of length \code{len}.
}
\description{
Sequential fast path for the common case of a block of consecutive
indices: \code{runif_seq(key, start, len)} returns exactly
\code{runif_at(key, start + 0:(len - 1))} — bit for bit the same values —
but without building, reading or validating an index vector, and
emitting four values per Philox call throughout. Use it for bulk
generation; use the \verb{*_at()} forms for scattered indices.
}
\details{
There is deliberately no \code{by} argument: strides of 4 or more share no
Philox blocks, so a strided draw has no fast path — write it as
\code{runif_at(key, seq(start, by = ..., length.out = ...))} instead.
}
\examples{
key <- rng_key(42)
identical(runif_seq(key, 1, 10), runif_at(key, 1:10))
runif_seq(key, 1e6, 5)   # 5 values starting at index 1e6
}
