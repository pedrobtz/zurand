% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zurand.R
\name{rng_uniform}
\alias{rng_uniform}
\title{Draw uniform random values}
\usage{
rng_uniform(key, n = 1L, min = 0, max = 1)
}
\arguments{
\item{key}{An \code{rng_key} vector.}

\item{n}{A single non-negative whole number. Defaults to one draw per key.}

\item{min, max}{Single finite numeric bounds.}
}
\value{
A numeric vector of length \code{n} for one key, or an \code{n} by
\code{length(key)} numeric matrix for multiple keys.
}
\description{
Stateless uniform sampling. \code{rng_uniform(key, n, min, max)} is a pure
function of its arguments and returns ordinary numeric vectors or matrices.
}
\details{
With the default bounds every value lies strictly inside \eqn{(0, 1)}: it
is \eqn{(m + 1/2) 2^{-52}} for a 52-bit integer \eqn{m}, so neither
endpoint can occur. Other bounds are applied as \code{min + (max - min) * u},
which, as with \code{\link[stats:runif]{stats::runif()}}, will not return either extreme value
unless \code{max = min} or \code{max - min} is small compared with \code{min}.
}
\examples{
key <- rng_key(42L)
rng_uniform(key)
rng_uniform(key, 5L)
rng_uniform(rng_key(42L, n = 3L), 5L)
}
