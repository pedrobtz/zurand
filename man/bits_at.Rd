% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rngat.R
\name{bits_at}
\alias{bits_at}
\alias{runif_at}
\alias{rnorm_at}
\title{Draw random values at arbitrary indices}
\usage{
bits_at(key, index, domain = NULL)

runif_at(key, index, domain = NULL)

rnorm_at(key, index, domain = NULL)
}
\arguments{
\item{key}{An \code{rngat_key}, from \code{\link[=rng_key]{rng_key()}} or \code{\link[=fold_in]{fold_in()}}.}

\item{index}{Vector of whole numbers: the positions to evaluate.}

\item{domain}{Optional vector of whole numbers selecting an independent
stream for the same indices (default \code{NULL} is domain 0). Either a
single value or the same length as \code{index}.}
}
\value{
A double vector as long as \code{index} (or \code{domain}, if \code{index}
has length 1).
}
\description{
Random access random numbers: each value is a pure function of
\verb{(key, index, domain)}, computed with the Philox4x64-10 counter-based
generator from the Random123 library. Evaluating the same triple always
gives the same value, in any order, on any platform — there is no
generator state to set, advance or restore.
}
\details{
\itemize{
\item \code{bits_at()} returns raw random bits as a 32-bit unsigned integer
value (stored in a double, range 0 to 2^32 - 1).
\item \code{runif_at()} returns doubles strictly inside (0, 1), built from the
top 53 bits of the generator output.
\item \code{rnorm_at()} returns standard normal deviates via inversion, so
\code{rnorm_at(k, i)} is identical to \code{qnorm(runif_at(k, i))}.
}
}
\examples{
key <- rng_key(42)
runif_at(key, 1:5)
runif_at(key, 3)              # element 3, without generating 1:2
runif_at(key, 1:5, domain = 1) # an unrelated stream at the same indices
rnorm_at(key, 1:5)
}
