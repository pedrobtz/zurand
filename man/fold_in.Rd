% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rngat.R
\name{fold_in}
\alias{fold_in}
\title{Derive a new key by folding data into an existing key}
\usage{
fold_in(key, identity)
}
\arguments{
\item{key}{An \code{rngat_key}, from \code{\link[=rng_key]{rng_key()}} or \code{\link[=fold_in]{fold_in()}}.}

\item{identity}{A single whole number distinguishing the derived key.}
}
\value{
A new \code{rngat_key}.
}
\description{
Deterministically derives a new, statistically independent key from
\code{key} and \code{identity}. Use this to give substreams of a computation
(chains, workers, blocks) their own keys without coordinating between
them: \code{fold_in(key, i)} for distinct \code{i} yield unrelated streams.
}
\details{
Key derivation is domain-separated from value draws: keys produced by
\code{fold_in(key, i)} share no bits with values drawn at index \code{i}.
}
\examples{
key <- rng_key(42)
chain_keys <- lapply(1:4, function(i) fold_in(key, i))
}
