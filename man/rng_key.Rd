% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zurand.R
\name{rng_key}
\alias{rng_key}
\alias{format.rng_key}
\alias{print.rng_key}
\alias{length.rng_key}
\alias{[.rng_key}
\alias{[[.rng_key}
\alias{c.rng_key}
\title{Create a stateless RNG key}
\usage{
rng_key(seed, n = 1L, engine = c("xoshiro256pp", "philox4x64", "threefry4x64"))

\method{format}{rng_key}(x, ...)

\method{print}{rng_key}(x, ...)

\method{length}{rng_key}(x)

\method{[}{rng_key}(x, i, ..., drop = FALSE)

\method{[[}{rng_key}(x, i, ...)

\method{c}{rng_key}(..., recursive = FALSE)
}
\arguments{
\item{seed}{A single non-negative whole number.}

\item{n}{A single non-negative whole number: how many keys to create.}

\item{engine}{The engine. All three give the same guarantees: a value is
a pure function of the key and the draw index, the same key gives the
same values on every platform and thread count, and draw \code{i} does not
depend on \code{n}. They differ in speed and in the stream they produce.
\itemize{
\item \code{"xoshiro256pp"} (default) -- the fastest: about \strong{2.4x} dqrng on
uniform and \strong{1.9x} RcppZiggurat on normal (single thread, x86_64,
with AVX2). Philox derives a fresh 256-bit xoshiro256++ state for each
512-value chunk, then a cheap recurrence produces the chunk, so a
chunk depends only on the key and its index. Reaching an arbitrary
index costs at most 511 recurrence steps. Audited with PractRand to
1 TB, including interleaved streams of sibling and folded keys.
\item \code{"philox4x64"} -- Philox4x64-10 from Random123, counter-based: each
value is computed directly from its position.
\item \code{"threefry4x64"} -- Threefry4x64-13 from Random123, counter-based;
about 1.1x Philox in practice.
}

A key records its engine, so a key made with one never produces the
other's values. Changing engine changes every number you get.}

\item{x}{An \code{rng_key}.}

\item{...}{Unused.}

\item{i}{Integer or logical index selecting keys. Missing values and
fractional numbers are an error: a silently invented key would
deterministically collide with every other key subset the same way.}

\item{drop}{Ignored; key subsetting always preserves the \code{rng_key} class.}

\item{recursive}{Ignored. Included for compatibility with \code{\link[=c]{c()}}.}
}
\value{
An object of class \code{rng_key} containing \code{n} keys.
}
\description{
\code{rng_key()} creates immutable keys for the stateless generators in this
package. A key vector is an opaque S3 object with one key per row; pass it to
\code{\link[=rng_fold]{rng_fold()}} and the \verb{rng_*()} samplers, but do not rely on its internal
integer representation.
}
\details{
The samplers in this package never read or update \code{.Random.seed}. Calling a
sampler twice with the same key and arguments returns the same values.
}
\section{Stream version}{

A key also records the version of the stream definition it was made
under, as \code{attr(key, "stream")}; every key made by this version of
zurand is stream \code{1L}. If a defect in a stream is ever found after
release, the fix becomes a new stream version for new keys, and existing
keys -- including saved ones -- keep producing exactly the values they
always did. A key saved before the attribute existed is treated as
stream 1. A key from a newer stream than this version of zurand
implements is an error rather than a silent reinterpretation.
}

\examples{
key <- rng_key(42L)
keys <- rng_key(42L, n = 4L)
key
}
